{
  "_comment": "Invisible marker — monsters teleport here. Tagged so the spawn rule can pick a random one each tick.",
  "definitions": [
    {
      "id": "spawn_marker",
      "tags": ["spawn_marker", "meta"],
      "properties": {},
      "state_init": {},
      "visual": {
        "shape": "invisible_marker",
        "params": {"color": "#00000000"}
      }
    }
  ]
}
