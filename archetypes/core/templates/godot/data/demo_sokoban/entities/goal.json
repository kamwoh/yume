{
  "_comment": "Goal tile. Visual marker only — gameplay logic checks if a box overlaps. occupied flag toggled by win-check rule.",
  "definitions": [
    {
      "id": "goal",
      "tags": ["goal"],
      "properties": {},
      "state_init": {
        "occupied": 0
      },
      "visual": {
        "shape": "filled_circle",
        "params": {"radius": 10, "color": "#dab048"}
      }
    }
  ]
}
