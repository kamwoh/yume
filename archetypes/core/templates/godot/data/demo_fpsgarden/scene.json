{
  "_comment": "FPS Garden scene. First-person 3D camera attached to player. Mouse-look (no pitch). Uses world-unit positions (1 unit = 1 meter), so renderer.position_scale=1.0 (default 0.05 is for 2D pixel-coord games).",

  "tick_seconds": 0.05,

  "renderer": {
    "position_scale": 1.0
  },

  "camera": {
    "mode": "first_person_3d",
    "follow_tag": "player",
    "eye_height": 1.6,
    "mouse_sensitivity": 0.003,
    "use_pitch": false,
    "fov": 70
  }
}
