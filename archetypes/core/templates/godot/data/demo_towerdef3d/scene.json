{
  "_comment": "TowerDef3D scene config. Top-down 3D camera looking at the path field. Tick 0.1s — TDs are deliberately slower than shooters. position_scale=1.0 because positions are world units.",

  "tick_seconds": 0.1,

  "renderer": {
    "position_scale": 1.0
  },

  "camera": {
    "mode": "top_down_3d",
    "follow_tag": "wave_clock",
    "height": 22,
    "fov": 60
  }
}
