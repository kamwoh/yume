{
  "_comment": "Auto-firing tower. cooldown ticks down each frame; when ≤ 0, tower fires at first enemy in range (6 m). properties.range is read by tower_fires rule (currently must be hardcoded in rule.query.radius — engine doesn't support per-entity contact radius yet).",
  "definitions": [
    {
      "id": "tower_basic",
      "tags": ["tower", "tower_basic"],
      "properties": {
        "fire_range": 6.0,
        "fire_cooldown": 0.5,
        "damage": 1,
        "level": 1
      },
      "state_init": {
        "cooldown": 0.0
      },
      "visual": {
        "mesh": "tower_3d",
        "params": {"base": "#404048", "barrel": "#a0a0b0", "glow": "#ffe060"}
      }
    }
  ]
}
