{
  "_comment": "Path turn-point. Each waypoint stores the velocity to apply to enemies that touch it (toward the next waypoint). state.next_vx and state.next_vz are set per-instance in zz_instances.json (computed from path geometry by content-designer).",
  "definitions": [
    {
      "id": "waypoint",
      "tags": ["waypoint"],
      "properties": {},
      "state_init": {
        "next_vx": 0.0,
        "next_vz": 0.0
      },
      "visual": {
        "mesh": "wp_marker",
        "params": {"glow": "#3a8a3a"}
      }
    }
  ]
}
