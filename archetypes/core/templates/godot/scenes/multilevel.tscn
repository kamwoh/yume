[gd_scene load_steps=4 format=3]

; demo_multilevel — 3-chamber navigation puzzle (ADR 0006 validation).
; Top-down 3D camera. Walk through walls, touch goal pad → next level.

[ext_resource type="Script" path="res://scripts/engine/world.gd" id="1"]
[ext_resource type="Script" path="res://scripts/engine/game_shell.gd" id="2"]

[sub_resource type="ProceduralSkyMaterial" id="SkyMat_1"]
sky_horizon_color = Color(0.4, 0.45, 0.55, 1)
ground_horizon_color = Color(0.15, 0.18, 0.22, 1)

[sub_resource type="Sky" id="Sky_1"]
sky_material = SubResource("SkyMat_1")

[sub_resource type="Environment" id="Env_1"]
background_mode = 2
sky = SubResource("Sky_1")
ambient_light_source = 3
ambient_light_color = Color(0.7, 0.7, 0.8, 1)
ambient_light_sky_contribution = 0.5

[node name="World" type="Node"]
script = ExtResource("1")
data_root = "res://data/demo_multilevel"
auto_start = true
verbose = true
renderer_script = "res://scripts/renderer_3d/entity_mesh_3d.gd"

[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Env_1")

[node name="Sun" type="DirectionalLight3D" parent="."]
transform = Transform3D(0.7, -0.3, 0.6, 0, 0.866, 0.5, -0.6, -0.35, 0.7, 0, 8, 0)
light_color = Color(1, 0.95, 0.85, 1)
light_energy = 0.9
shadow_enabled = true

[node name="GameShell" type="Node" parent="."]
script = ExtResource("2")

[node name="Camera3D" type="Camera3D" parent="."]
position = Vector3(0, 16, 6)
rotation = Vector3(-1.2, 0, 0)
