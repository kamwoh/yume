{
  "_comment": "Sokoban scene config — top-down 2D grid. tick_seconds=0 means 'tick on input only' (turn-based). Cell size = 32px. position_scale=1.0 because we author in pixels.",

  "tick_seconds": 0.1,

  "renderer": {
    "position_scale": 1.0
  },

  "camera": {
    "_": "Center on floor bbox + auto-zoom-to-fit so the whole level is visible regardless of size. fit_padding=64 leaves a margin of 2 cells. lerp=1.0 + zoom_lerp=1.0 = instant snap (turn-based — no smoothing needed). Updates per-frame so transitions reframe automatically.",
    "center_on_tag": "floor",
    "fit_padding": 64,
    "lerp": 1.0,
    "zoom_lerp": 1.0
  }
}
