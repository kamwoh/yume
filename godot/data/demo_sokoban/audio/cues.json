{
  "_comment": "Sokoban audio cue table (ADR 0009 Phase 2b). Maps semantic event names to concrete sound names from data/sounds.json. Rules emit `@cues.<event>`; engine resolves through this table. Swap entries here to change audio palette without touching rules.",

  "cues": {
    "wood_step": "build",
    "wood_push": "shotgun_blast",
    "level_won": "win",
    "level_restart": "build"
  }
}
