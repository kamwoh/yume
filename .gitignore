# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
venv/
.venv/

# IDE
.vscode/
.idea/
*.swp

# Project planning (findings/progress are session-specific)
findings.md
progress.md
session_log.txt
.vqa.log

# Capture screenshots — local artifacts, regenerable via play.sh --capture
captures/

# Per-game demo content + per-game scene launchers — generate locally via
# /yume-design or copy from upstream. The framework keeps only:
#   godot/scenes/play.tscn         (universal launcher with --game= arg)
#   godot/scenes/test_main.tscn    (engine unit tests)
#   godot/scenes/scenario_test.tscn (per-game scenario test runner)
# And shared engine data libraries:
#   godot/data/shapes.json, meshes.json, sounds.json
godot/data/demo_*/
godot/data/entities.json
godot/scenes/*.tscn
!godot/scenes/play.tscn
!godot/scenes/test_main.tscn
!godot/scenes/scenario_test.tscn
!godot/scenes/asset_preview.tscn

# Per-game design docs (GDD, world-plan, level-design, reviews, QA reports).
# These are local artifacts produced by /yume-design pipeline; they pair with
# the gitignored godot/data/demo_<name>/ folders.
docs/games/

# User saves (Godot user:// resolves to platform appdata, but if anyone
# sets data_root to a tracked path, this prevents leakage)
godot/data/*/saves/

# Generated output
output/

# Archive (old code, not needed)
_archive/

# Raw scraped data (large, copyrighted)
docs/ff9_raw/

# Environment
.env
.env.local

# Personal Claude Code config — hooks/permissions are machine- and user-specific
# (absolute paths, learning logs, other-project references). Contributors bring
# their own; the shared project config lives in CLAUDE.md + .claude/rules/ +
# .claude/skills/.
.claude/settings.json
.claude/settings.local.json
.claude/error_log.jsonl

# OS
.DS_Store
Thumbs.db

# Visual QA reports — per-run artifacts, regenerate via tools/visual_qa/run_plan.py
tools/visual_qa/sample_plans/visual_test_report/
tools/visual_qa/**/visual_test_report/
tools/visual_qa/plans/

# ADR 0058: compiled shaders from tools/yume_shadergen — regenerate from scene.json shader_template + template
godot/data/demo_*/assets/shaders/

# Generated/AI assets — NEVER commit (binary bloat + paid Tripo/gen artifacts).
# Regenerate via the asset pipeline (tools/yume_assetgen + compose_*).
# Per-game demo_*/ is already ignored above; these cover the shared lib +
# any other generated texture/mesh/concept output.
godot/data/lib/textures/*
# .tres files in lib/textures are source-controlled Godot resources
# (e.g. NoiseTexture2D resource definitions), NOT generated assets.
# (Must use `dir/*` instead of `dir/` so negation re-includes work.)
!godot/data/lib/textures/*.tres
godot/data/**/assets/concepts/
*.glb.import

# Local video recordings (net_video.py) — large binaries, regenerated
recordings/

# Generated doc HTML (no committed doc-viewer anymore). The timeline/ viewer is
# kept (docs/timeline/index.html is in a subdir, so these globs don't touch it).
docs/*.html
docs/adr/*.html

# ── OSS example games — committed so a fresh `git clone` has runnable demos ──
# (README § Generation pipeline · CONTRIBUTING). sokoban + doomarena3d are
# code-draw / asset-free; demo_lanterns ships a slimmed low-poly mesh set.
!godot/data/demo_sokoban/
!godot/data/demo_sokoban/**
!godot/data/demo_doomarena3d/
!godot/data/demo_doomarena3d/**
!godot/data/demo_lanterns/
!godot/data/demo_lanterns/**
# ...but never the _snapshots/ backup dirs inside the example demos.
godot/data/demo_sokoban/_snapshots/
godot/data/demo_doomarena3d/_snapshots/
godot/data/demo_lanterns/_snapshots/
# demo_lanterns ships RUNTIME assets only (low-poly meshes + the heightmap/
# splatmap maps + the player glb). The AI-gen concept + reference image dirs
# are authoring scratch, not needed to run — keep them out of the repo.
godot/data/demo_lanterns/assets/concepts/
godot/data/demo_lanterns/assets/reference/
