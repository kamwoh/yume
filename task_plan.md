# Yume — task plan (index)

_Split 2026-05-30. The single 5k-line `task_plan.md` became a topic-split set
under `.claude/plan/` so the live backlog stays readable and
`/planning-with-files` can operate on focused files._

- **Live backlog** → [`.claude/plan/backlog.md`](.claude/plan/backlog.md)
  — the actionable TODOs only. Start here.
- **History + decision log** → [`.claude/plan/archive.md`](.claude/plan/archive.md)
  — the full former task_plan (roadmaps, session wraps, decision diary). Append
  major decisions there; don't rewrite.

This stub stays at the repo root so existing references (CLAUDE.md,
`/planning-with-files`) still resolve.
